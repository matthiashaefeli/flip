<%= form_with model: @comment do |form| %>
  <div class="form_fields">
    <div><%= form.text_field :title, placeholder: 'Title' %></div>
    <div><%= form.rich_text_area :content %></div>
    <div><%= form.submit %></div>
  </div>
  <%= form.hidden_field :project_id, value: @object.id %>
<% end %>