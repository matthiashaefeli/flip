<%= form_with model: @project do |form| %>
  <%= form.text_field :name %>
  <%#= form.collection_select(:project, :project_type_id, ProjectType.all, :id, :name, prompt: true) %>
  <%= form.collection_select(:project_type_id, ProjectType.all, :id, :name) %>
  <%= form.submit %>
<% end %>