<%= form_with model: @finance do |form| %>
  <%= form.text_field :name, placeholder: 'Name' %>
  <%= form.select(:ftype, Finance.ftypes.keys) %>
  <%= form.submit %>
  <%= form.hidden_field :project_id, value: @project.id %>
<% end %>