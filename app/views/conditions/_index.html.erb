<h1>Condtitions:</h1>
<p><%= link_to 'New Condition', new_condition_path(project: @project) %></p>
<table>
  <thead>
    <tr>
      <td>Name</td>
      <td>Status</td>
      <td>Initial Condition</td>
      <td>Edit</td>
      <td>Delete</td>
    </tr>
  </thead>
  <tbody>
    <tr>
    <% @project.conditions.each do |condition| %>
      <td><%= condition.name %></td>
      <td><%= condition.status %></td>
      <td><%= condition.initial_condition %></td>
      <td><%= link_to 'edit', edit_condition_path(condition) %></td>
      <td><%= link_to 'delete', condition_path(condition), method: "delete" %></td>
    <% end %>
    </tr>
  </tbody>
</table>